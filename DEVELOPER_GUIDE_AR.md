# دليل المطور - إدارة معرض الأعمال (Portfolio)

أهلاً بك في دليل المطور الخاص بموقعك الاحترافي. هذا الملف هو مرجعك الشامل لإضافة أو تعديل أو إزالة أي محتوى في الموقع بنفسك دون الحاجة للرجوع لمبرمج، لأن الموقع مصمم ليكون ديناميكياً يقرأ البيانات من مصفوفات (Arrays) بسيطة.

جميع التعديلات على المحتوى (المشاريع، الخدمات، الآراء، الخبرات) تتم من خلال ملف واحد فقط وهو: **`script.js`**.

---

## 1. إدارة المشاريع (Projects)

يتم تخزين جميع المشاريع في مصفوفة تُسمى `projects` في بداية ملف `script.js`.
العداد الموجود في الموقع يقرأ عدد هذه المشاريع تلقائياً (ويضيف عليها 20 مشروع إضافي كما طلبنا).

### 💡 كيف تضيف مشروعاً جديداً؟
ابحث في ملف `script.js` عن كلمة `const projects = [`.
في نهاية القائمة (قبل القوس المربع الأخير `]`)، أضف الكود التالي للمشروع الجديد مع تغيير بياناته:

```javascript
{
    id: 10, // رقم فريد لا يجب أن يتكرر
    title_en: "Project Name Here",
    title_ar: "اسم المشروع بالعربي",
    category: "Data Visualization & Dashboard", // يجب أن يطابق أحد الأقسام الموجودة في الفلتر
    desc_en: "Short description in English.",
    desc_ar: "وصف قصير يظهر على الكارت من الخارج.",
    details_en: "Full long details for the popup modal.",
    details_ar: "وصف طويل ومفصل يظهر عند الضغط على تفاصيل المشروع.",
    enhancements_en: "Future enhancements...",
    enhancements_ar: "التطويرات المستقبلية...",
    thumbnail: "projectX/image_name.png", // مسار الصورة
    videoSrc: null, // ضع رابط فيديو هنا إذا وجد، أو اتركه null
    githubLink: "https://github.com/your-link" // رابط المشروع
}
```

> **ملاحظة هامة:** تأكد من وضع فاصلة `,` بعد المشروع الذي يسبقه. الأقسام (Category) المتاحة حالياً هي: `Data Visualization & Dashboard`، `Data Analysis`، `Exploratory Data Analysis`، `Reporting Automation`. السبيس والحروف مهمة لكي يعمل الفلتر.

---

## 2. إدارة الخدمات (Services)

تتم إدارة الخدمات في مصفوفة `services` في ملف `script.js`.

### 💡 كيف تضيف خدمة جديدة؟
لإضافة خدمة، ابحث عن `const services = [`، وأضف خدمة جديدة في القائمة بهذا الشكل:

```javascript
{
    id: 's4', // معرف فريد
    category: "Data Analytics & AI", // يجب أن يطابق قسم الفلتر
    title_en: "Service Title",
    title_ar: "عنوان الخدمة بالعربي",
    desc_en: "Short description.",
    desc_ar: "وصف قصير يظهر في الخارج.",
    icon: "fas fa-database", // أيقونة من موقع FontAwesome
    details_en: "Long service details.",
    details_ar: "تفاصيل الخدمة الطويلة.",
    links: [ // أزرار التواصل أو الشراء داخل تفاصيل الخدمة
        { 
            name: "Direct", 
            url: "https://wa.me/201148295790", 
            icon: "btn-direct", 
            label_en: "Direct Request", 
            label_ar: "طلب مباشر" 
        }
    ]
}
```

---

## 3. آراء العملاء (Testimonials)

تُدار آراء العملاء عبر مصفوفة `testimonials` في `script.js`.

### 💡 كيف تضيف رأي عميل جديد؟
ابحث عن `const testimonials = [` وأضف الكود التالي:

```javascript
{
    name_en: "Client Name",
    name_ar: "اسم العميل",
    role_en: "CEO",
    role_ar: "المدير التنفيذي",
    content_en: "English review text.",
    content_ar: "نص التقييم أو الرأي بالعربي.",
    avatar: "https://i.pravatar.cc/150?u=client_name" // رابط الصورة، يمكنك تركه كما هو وسيولد صورة عشوائية للمناظر
}
```

---

## 4. الخبرات المهنية (Experience) والشهادات (Certificates)

نفس الفكرة بنسبة 100%. ستجد مصفوفة `experience` للخبرات ومصفوفة `certifications` للشهادات.
يمكنك إضافة أقواس جديدة `{ ... }` بداخلها لتحديث بياناتك تلقائياً وبكل اللغات.

---

## 5. الإحصائيات (Hero Section)

- **سنوات الخبرة**: يتم حسابها بشكل ديناميكي (السنة الحالية - 2023)، بحد أدنى 3 سنوات. يمكنك التعديل على متغير `startYear` في الدالة `updateDynamicStats()`.
- **عدد المشاريع**: يتم حسابه ديناميكياً من عدد المشاريع في المصفوفة `projects` مقروءاً بزيادة 20 مشروعاً إضافياً (`projects.length + 20`). 

---

## نصائح عامة لتجنب الأخطاء
1. **الفواصل (Commas):** تأكد دائماً من وجود فاصلة `,` بين كل كائن `{}` وآخر في المصفوفات. آخر كائن لا يحتاج لفاصلة بعده.
2. **الرموز:** إذا كان المسار يحتوي على مسافات، يفضل استبدالها في المسار أو استخدام `%20`.
3. **الأيقونات:** يمكنك استخدام أي أيقونة مجانية من موقع (FontAwesome 5) ووضع الكلاس الخاص بها في حقل `icon`.

*تم إعداد هذا الدليل بواسطة Antigravity AI لضمان إدارة سريعة وفعّالة لموقعك.*
